[project]
name = "chroma-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chromadb>=1.5.9",
    "ipython>=9.16.1",
    "jupyter>=1.1.1",
]

[tool.uv]
link-mode = "copy"
